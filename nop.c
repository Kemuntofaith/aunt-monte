#include "monty.h"

/**
 * nop - this reminds me of my ex(useless!)
 * @stack: may he burn with a pop sound!
 * @line_cnt : counting days till he explode >>>>
 *
 * Return: NOTHING
 */
void nop(stack_t **stack, unsigned int line_cnt)
{
	(void) stack;
	(void) line_cnt;
}
